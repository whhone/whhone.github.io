baseURL = "https://whhone.com/"
languageCode = "en-us"
title = "Wai Hon's Blog"

# Google Analytics API key.
googleAnalytics = "UA-38001261-1"

# Disqus Integration. See https://gohugo.io/content-management/comments/.
disqusShortname = "wai-hons-blog"

# Permalinks is the permanent link to a post. It is supposed to be permanent and
# as simple as possible.
#
# - The file name ("/:filename/") is used because it could be permanent is
#   simple.
# - The date ("/:year/:month/") is not being used because it makes the link
#   longer. If the post is time-related, we can always include the time in the
#   file name.
# - The title (":title") is not being used because it could be changed after
#   publishing the post.
[permalinks]
  posts = "/:filename/"

# Theme's configuration.
#
# See https://github.com/mismith0227/hugo_theme_pickles/blob/release/exampleSite/config.toml
[params]
  favicon = "/ico/favicon.ico"
  avatar = "/img/avatar.jpg"
  author = "Wai Hon Law"
  bio = "Software Engineer, Runner, Husband, Father."
  email = "whhone@gmail.com"

  twitter = "whhone"
  github = "whhone"
  facebook = "whhone"

[[menu.main]]
  name = "Home"
  url = "/"
  weight = 2

[[menu.main]]
  name = "RSS"
  url = "/index.xml"
  weight = 10
